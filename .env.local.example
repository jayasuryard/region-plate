# API URLs for connecting to the backend server

# Development - use your computer's local IP address  
# Find it with: `ipconfig getifaddr en0` (Mac) or `ipconfig` (Windows)
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api

# Production
# EXPO_PUBLIC_API_URL=https://api.regionplate.com/api
